#
# Author:linrol
#

version: '2'

services:
  coolq-pro:
    image: richardchien/cqhttp
    restart: always
    container_name: coolq-pro
    volumes:
      - /root/web/app/coolq/cooq-pro:/home/user/coolq
    environment:
      - VNC_PASSWD=${VNC_PASSWD}
      - COOLQ_URL=https://dlsec.cqp.me/cqp-tuling
      - COOLQ_ACCOUNT=${COOL_QQ}
      - CQHTTP_USE_HTTP=false
      - CQHTTP_USE_WS_REVERSE=true
      - CQHTTP_WS_REVERSE_URL=${WS_URL}
      - CQHTTP_WS_REVERSE_UNIVERSAL_CLIENT=true
    ports:
      - ${VNC_PORT}:9000
  coolq-air:
    image: richardchien/cqhttp
    restart: always
    container_name: coolq-air
    volumes:
      - /root/web/app/coolq/cooq-air:/home/user/coolq
    environment:
      - VNC_PASSWD=${VNC_PASSWD}
      - COOLQ_URL=http://dlsec.cqp.me/cqa-tuling
      - COOLQ_ACCOUNT=${COOL_QQ}
      - CQHTTP_USE_HTTP=false
      - CQHTTP_USE_WS_REVERSE=true
      - CQHTTP_WS_REVERSE_URL=${WS_URL}
      - CQHTTP_WS_REVERSE_UNIVERSAL_CLIENT=true
    ports:
      - ${VNC_PORT}:9000
  


